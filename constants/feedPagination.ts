/** Page size for list/people discovery feeds (matches backend ListSearchAPIView). */
export const FEED_PAGE_SIZE_LISTS = 25;

/** Page size for pick discovery feeds (matches backend ListItemsAPIView). */
export const FEED_PAGE_SIZE_PICKS = 30;

/** Max candidates scored/paginated — mirrors backend HISTOGRAM_POOL. */
export const FEED_MAX_POOL = 200;
